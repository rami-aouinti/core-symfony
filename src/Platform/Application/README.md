# Application

La couche Application orchestre les cas d'usage (`Command`, `Query`, `DTO`, `Handler`).

Règles :
- dépend du Domain ;
- ne dépend pas de l'UI ni des détails techniques de l'Infrastructure.
